# 董事会研究与决策运行内核

本文件是董事会召开时的唯一主协议。只有用户明确说出“启动董事会决策”后才读取和执行。

## 1. 运行架构

```text
用户目标
  → 协调者 Agent：定义目标、研究事实、拆任务、选成员
  → 入选成员并行独立研究
  → 综合决策 Agent：去重、补方案、比较、挑战
  → 当前证据与约束下的最优可执行解
```

系统角色与人格成员职责分离：协调者不预先决定答案，成员不直接决定最终答案，综合决策 Agent 不按票数或名气选方案。

## 2. 最小读取路径

1. 读取本文件和 [协调者 Agent](系统Agents/协调者Agent.md)。
2. 协调者建立共享决策包与调度计划。
3. 读取 [成员路由索引](成员Skills/README.md)，只根据路由字段选人。
4. 首轮只向每名入选成员发送“任务信封 + 自己的 `议事卡.md`”。
5. 汇聚首轮结果后，读取 [综合决策 Agent](系统Agents/综合决策Agent.md)。
6. 仅对能改变最优解判断的成员读取其 `SKILL.md`，最多进行一次定向补充研究。

默认不读取 `原包/`、`来源与审查.md`、`蒸馏来源/`、`架构参考/`、历史决策或其它董事会说明文件。

## 3. 共享决策包

协调者只创建一份带编号的决策包，成员引用编号，不复述背景：

- `Q`：必须决定的问题。
- `O1...On`：已有候选方案，包含维持现状。
- `C1...Cn`：判断何为“更优”的成功标准和优先级。
- `F1...Fn`：已核验且与决策直接相关的事实。
- `A1...An`：关键假设与未知。
- `R1...Rn`：需要研究才能决定的问题。
- `B1...Bn`：硬约束、不可接受边界和时限。
- `V1...Vn`：用户价值取向。

没有 `C/B/V` 就不能声称选出最优解。关键事实缺失时先研究；普通缺失信息明确列为假设并继续。

## 4. 协调与并行分发

默认使用标准模式；用户指定人数或模式时优先服从。

| 模式 | 首轮成员数 | 深评成员数 | 适用情况 |
| --- | ---: | ---: | --- |
| 快速 | 3–5 | 1–2 | 可逆、时间紧、选项清楚 |
| 标准 | 6–10 | 2–4 | 默认重要决策 |
| 广泛 | 10–20 | 3–5 | 高影响、跨领域、需要广泛找盲区 |

- 每名成员必须有唯一任务和边际新增价值；不默认全员参与。
- 每名成员只接收核心 `Q/O/C/B/V`、与任务相关的 `F/A/R`、自己的议事卡和输出约束。
- 支持真实并行时使用独立 Agent；不支持时按逻辑并行处理，仍保持信息隔离。
- 成员在首轮看到其它意见前独立提交；无新增价值则输出 `PASS`。

## 5. 两阶段研究

### 阶段 A：广泛独立研究

每名成员最多 180 个汉字：

```text
立场：支持 O? / 反对 O? / 暂缓 / 提出 O? / PASS
独有判断：只写能改变决策的一条判断
依据与风险：引用 F/A/R/B 编号
反证信号：什么会改变本意见
最小动作：如何验证或推进
```

成员不得复述决策包、介绍自身模型、读取其它成员材料或重复已知观点。

### 阶段 B：定向深评与补充研究

协调者只选择能改变最优解判断的少数任务进入深评，优先处理：

1. 可能淘汰领先方案的事实缺口。
2. 低概率高损失风险。
3. 与主流结论冲突且依据不同的少数意见。
4. 新的第三方案、组合方案或最小可逆试验。

深评成员可读取自己的 `SKILL.md`。最多追加一次定向补充轮，禁止无止境讨论。

## 6. 综合决策与选优

综合决策 Agent 只接收共享决策包和标准化结果：

1. 淘汰 `PASS`、无依据、越界和重复意见。
2. 聚类共识、决定性分歧、关键风险和脆弱假设。
3. 补充第三方案、组合方案和分阶段方案。
4. 用 `B` 淘汰不可行方案。
5. 按 `C`、证据强度、风险、可逆性、机会成本、二阶影响和执行难度比较方案。
6. 对领先方案执行最强反方、失败预演和假设敏感性测试。
7. 通过质量门后选择一个当前最优解；若暂缓并先验证更优，也必须明确给出。

重复观点只算一条证据，不制造多数幻觉；没有用户认可的权重时不使用伪精确评分或虚构置信度。

## 7. 最优解质量门与交付

最终推荐必须：

- 满足硬边界，并说明为何优于替代方案。
- 区分事实、假设和未知，保留最强反方与失效信号。
- 给出最小可逆行动、执行步骤、验证指标、停止与回滚条件。
- 标明仍需补充核验或专业复核的事项。

默认只输出当前最优解及必要依据，不展示全员完整发言。未经明确要求，不写入决策记录。
