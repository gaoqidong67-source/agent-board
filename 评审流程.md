# 董事会评审流程

本文件解释 [运行内核](运行内核.md) 的设计，召开时不默认读取。架构分为协调者扇出、成员并行研究和综合决策汇聚三层。

## 1. 先压缩问题，不压缩关键不确定性

协调者把背景转成带编号的共享决策包：问题 `Q`、方案 `O`、标准 `C`、事实 `F`、假设 `A`、待研究问题 `R`、边界 `B`、价值 `V`。所有成员引用编号，避免重复背景。没有 `C/B/V`，综合决策 Agent 无法判断何为更优。

## 2. 协调者通过任务信封扇出

协调者只查看成员索引中的视角簇、入选条件、跳过条件和独有问题。每名成员得到一个独有任务、相关上下文编号和自己的议事卡，不得到其它成员材料。成员价值来自边际新增判断，不来自名气或人数。

## 3. 广泛轻评审负责找信号

首轮成员只读取自己的 `议事卡.md`，提交不超过 180 个汉字的压缩表决。成员不得介绍自己的模型、复述决策包或重复已有观点；没有新增内容就输出 `PASS`。

这一阶段允许容纳更多成员，目标是快速发现：

- 被遗漏的风险、选项和约束。
- 冲突的事实判断或价值权重。
- 共同依赖的脆弱假设。
- 能显著降低不确定性的最小实验。

## 4. 少数深评审负责解决关键未知

协调者只选择能改变最优解判断的少数成员读取完整 `SKILL.md`。深评围绕最多 3 个决定性分歧展开，要求给出依据链、最强反方、验证动作和改变意见的条件，不做全员互评。

## 5. 综合决策 Agent 负责选优

- 重复观点只保留一条，记录支持它的成员范围但不增加证据权重。
- 少数意见若指出低概率高损失风险，必须单独保留。
- 只有用户提供或认可权重时才做加权评分，避免伪精确数字。
- 对不可逆且证据不足的选择，优先设计试点、分阶段承诺和退出机制。
- 高风险专业决策必须核对当前权威来源，并标明专业复核需求。
- 综合决策 Agent 必须补充第三方案和组合方案、淘汰违反硬边界的方案、挑战领先方案，并只输出一个当前最优解。
