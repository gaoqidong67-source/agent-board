# 董事会成员管理规则

人格成员是供协调者 Agent 按需加载的“判断模型蒸馏 Skill”，不是普通任务中的独立角色扮演 Skill。成员负责独立研究和判断，不直接决定最终答案；最终选优由综合决策 Agent 完成。

## 放置与索引

1. 每个人格建立独立目录；`议事卡.md` 是首轮轻评审入口，`SKILL.md` 是少数成员深评入口。
2. `member-id` 使用小写字母、数字和连字符，方便迁移和校验。
3. 在 [成员路由索引](成员Skills/README.md) 中登记状态、视角簇、入选信号、独有问题、议事卡和深评入口。
4. 来源和边界未核验时标为“草稿”；只有“启用”成员可被选入董事会。
5. 人格成员不加入普通技能索引、不独立部署、不允许隐式触发。

## 状态口径

- 草稿：结构已建立，但来源、边界或差异性尚未核验，不可用于正式董事会。
- 启用：来源可追溯、边界清晰、议事卡具有独有问题，可按需选用。
- 停用：暂时不应使用，但保留历史内容与来源。
- 归档：已被替代或不再维护，默认不读。

## 第三方原包隔离

第三方人格 Skill 使用“董事会适配层 + 原包只读归档”结构：

```text
成员Skills/<member-id>/
├── 议事卡.md         # 首轮轻评审入口，短小且只提供独有视角
├── SKILL.md          # 深评适配层，仅少数成员按需读取
├── 来源与审查.md     # 来源提交、许可证、风险和限制
├── FILES.sha256      # 原包逐文件完整性清单
└── 原包/             # 完整第三方包，不含 .git，不改写
```

- 第三方原包在导入前必须完成安全审查；原包保持只读，不修改正文。
- 顶层适配层必须明确覆盖原包的隐式触发、角色扮演、工具权限和命令式工作流。
- 不执行 `原包/` 中的脚本、安装、联网、下载、测试、生成或写入流程。
- 启动董事会并选中成员后，首轮只读议事卡；只有进入深评才读适配层，只有来源核验时才按需读取原包。
- 共同蒸馏框架放入 `董事会/蒸馏来源/`，不登记为成员。

## 议事卡最低结构与预算

每张 `议事卡.md` 必须包含：

- `视角簇`
- `入选条件`
- `跳过条件`
- `独有问题`
- `重点风险`
- `反证信号`
- `首轮要求`

议事卡应控制在 16 行以内；必须要求没有新增判断时输出 `PASS`，首轮总计不超过 180 个汉字。议事卡不放来源说明、人物介绍、完整模型、案例或重复的安全规则。

## 深评 Skill 最低结构

成员 `SKILL.md` 的 frontmatter 只使用 `name` 和 `description`。`description` 必须明确包含：

- 仅供董事会选中后读取。
- 不得独立触发。
- 不代表真人发言或背书。

正文至少包含：

- `## 定位与边界`
- `## 来源基础`
- `## 核心判断模型`
- `## 评审问题`
- `## 常见盲区`
- `## 输出要求`

详细起稿结构见 [成员 Skill 模板](成员Skills/成员Skill模板.md)。

## 质量门槛

- 蒸馏内容必须可追溯，不得仅凭网络印象拼接。
- 区分直接来源、可靠二手来源和编写者推断。
- 同一成员的原则、适用边界和常见盲区必须同时记录。
- 不模仿真人语气，不伪造引语，不补写来源没有支持的观点。
- 原包要求直接扮演真人时，以董事会适配层的“不代表真人、不模仿语气”规则为准。
- 成员之间高度重复时，优先合并或保持“草稿”；同一视角簇成员必须在索引中体现不同独有问题。
- 深评 `SKILL.md` 不得被首轮批量读取；原包不得进入常规决策上下文。
- 成员输出必须符合标准任务信封，引用共享决策包编号；不得自行扩展问题、读取其它成员意见或把自己描述为最终决策者。
- 添加或修改成员后运行 `.\董事会\scripts\Test-BoardDirectory.ps1`。
